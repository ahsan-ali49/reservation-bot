{
  "name": "reservation-bot",
  "version": "1.0.0",
  "description": "This repo contains program that automates the browser to reserve the library space.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "puppeteer": "^21.11.0"
  }
}
